<div class="task">
  <h3><%= task.name %></h3>
  <p><%= task.details %></p>
  <p>Status: <%= task.status ? "Complete" : "Incomplete" %></p>
  <p>Due date: <%= task.due_date.strftime("%B %d, %Y") %></p>
</div>